/* import { useAxiosWrapper } from "./axiosWrapper";

// eslint-disable-next-line react-hooks/rules-of-hooks
const axiosWrapper = useAxiosWrapper();

export const useApi=()=>{
    return {
        getNotes,
    }

    function getNotes(code: string) {
        const body = {
          code: code
        }
        return axiosWrapper.get(`/`, body)
      }
    
} */